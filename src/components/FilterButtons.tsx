export default function FilterButtons(){
  return (
    <div className="filters">
      <button className="filter active">All</button>
      <button className="filter">Liquids</button>
      <button className="filter">Vapes</button>
    </div>
  )
}
